<h4>Browse the Artists</h4>
<h5>
<ul>
  <% @artists.each do |artist| %>
    <li> <%= link_to artist.name, [artist.chart, artist] %></li>
  <% end %>
</ul></h5>
<button class="silver btn-small"><%= link_to 'Create a new Artist', new_chart_artist_path(@chart) %></button>
